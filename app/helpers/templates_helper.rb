module TemplatesHelper
  def build_template(template)
  	# XXX - enable!!
    # template.hipswitches.build if template.hipswitches.empty?
    # template.deployments.build if template.deployments.empty?
    # template.hosts.build if template.hosts.empty?
    template
  end
end
